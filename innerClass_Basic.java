public class innerClass_Basic {
    class inner {
        // 인스턴스 내부 클래스
    }
    static class staticClass {
        // 정적 내부 클래스
    }
    void run() {
        class localClass {
            // 지역 내부 클래스
        }
    }
}
